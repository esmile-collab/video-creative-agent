# 公开仓库迁移记录

## 迁移结果

本仓库从现有视频 Agent（智能体）探索中提取出四类通用资产：策略召回、脚本请求编译、确认脚本到分镜、结构评测。实现经过重新命名和最小化整理，不依赖原工作区运行文件。

## 已迁移

- 策略卡的硬过滤、文本排序、Top-1（第一名）选择、人工覆盖和回退逻辑。
- 策略卡的 retrieval_view（召回视图）、generation_view（生成视图）与 transfer_boundary（迁移边界）。
- Brief（任务简报）、基础 Skill（生成规则）和策略卡的脚本请求编译。
- 确认脚本冻结、确定性分段、连续时间、画面类型与 Caption（画面描述）。
- 结构门禁、切点 F1（调和平均指标）、JSON（结构化数据）和 Markdown（可读文档）导出。
- 三条 C 端公开视频的 ID、标题和 URL（链接），只作为 Demo（演示）来源指针。

## 未迁移

- 消耗、曝光、点击、转化、客户、账户和其他私有业务数据。
- 内部接口、代码地址、鉴权信息、本机路径和运行会话记录。
- 视频、音频、完整字幕、抽帧、Office（办公文档）和历史实验包。
- 原始模型请求响应、未公开 Prompt（提示词）、人工标注全集和内部策略卡正文。

## 重新实现

- Demo（演示）Brief、脚本和两张策略卡均为公开仓库原创内容。
- Mock Provider（模拟模型供应方）支持无密钥离线运行。
- 脱敏扫描覆盖私有指标字段、凭证、内部域名、本机路径、大文件和媒体文件。

## 验证入口

```bash
npm run verify
```

该命令依次执行脱敏扫描、单元测试和离线 Demo（演示）。自动通过只代表仓库边界与结构检查通过，成片质量仍需人工评审。
