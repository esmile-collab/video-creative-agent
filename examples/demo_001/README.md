# Demo 001（演示 001）

这个案例用真实公开视频页面作为来源指针，用原创 Brief（任务简报）、策略卡和脚本演示完整工程链路。

`public-sources.json` 只包含公开视频 ID、标题和 URL（链接）。仓库没有缓存媒体，也没有收录完整字幕、抽帧、业务效果或内部运行数据。

运行：

```bash
npm run demo
```

输出位于 `outputs/demo_001/`。
