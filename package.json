{
  "name": "video-creative-agent-public",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node app/server.mjs",
    "demo": "node scripts/run-demo.mjs",
    "safety": "node scripts/check-public-safety.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run safety && npm test && npm run demo"
  }
}

